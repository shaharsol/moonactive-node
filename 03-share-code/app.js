// the name of the import/export methodolofy
// invented by Nodejs is called CommonJS
// and was widely used until node12
const { sum, mul } = require('./calc')

console.log(sum(1, 2))
console.log(mul(1, 2))
console.log(sub(1, 2))
